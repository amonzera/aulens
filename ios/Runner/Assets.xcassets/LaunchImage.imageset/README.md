# Launch Screen Assets

This directory contains the iOS launch screen image assets for Aulens.

To customize the launch screen, replace the image files in this directory or open `ios/Runner.xcworkspace` in Xcode and update `Runner/Assets.xcassets`.
